# Fake News Live Detector (Google News + Streamlit + ML)

## Setup
1. Create project folder and add files from this repo.
2. Create a Python virtual environment:
   ```bash
   python -m venv venv
   # Windows
   venv\Scripts\activate
   # macOS / Linux
   source venv/bin/activate
